version: '2'

services:
  filebeat:
    build: ./
    volumes:
      - ./config/filebeat.yml:/usr/share/filebeat/filebeat.yml
      - /tmp/data:/mnt/data
    networks:
      - esnet
    #depends_on:
    #  - elasticsearch
    #  - logstash

networks:
  esnet:
    driver: bridge
